'use strict';

var React = require('react-native'),
    { Text, View } = React;

var Roster = React.createClass({
    render: function() {
        return (
            <View>
                <Text>This is a Roster!</Text>
            </View>
        );
    }
});

module.exports = Roster;
